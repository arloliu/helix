simulation:
  duration: 5m
  seed: 42
  console_interval: 5s

helix:
  write_strategy:
    delta_threshold: 100ms
    strike_threshold: 3

  replay:
    queue_size: 10000

workload:
  workers: 1
  interval: 10ms
  payload_size: 100
  read_ratio: 0.2
  batch_ratio: 0.1
